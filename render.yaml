services:
  - type: web
    name: wastehub-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn wastehub_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
    plan: free

